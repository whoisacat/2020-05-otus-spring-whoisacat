spring:
  datasource:
    initialization-mode: always
    url: jdbc:h2:mem:testdb-test
    data: classpath:/data.sql
  jpa:
    show-sql: true
    generate-ddl: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate
